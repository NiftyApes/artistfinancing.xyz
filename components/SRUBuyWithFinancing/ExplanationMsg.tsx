import { FC } from 'react'

const ExplanationMsg: FC = () => {
  return (
    <div>
      In escrow until payments completed, and can be seized in event of default
    </div>
  )
}

export default ExplanationMsg
